{{outlet}}
{{#each model as |project|}}
	{{#paper-list}}
{{#paper-item}}
   {{paper-icon "description"}}
 <p {{action "transitionToProjectView" project}}>{{project.teamName}}</p>

 
 {{paper-divider}}
 
     {{/paper-item}}
    {{/paper-list}}
    {{/each}}